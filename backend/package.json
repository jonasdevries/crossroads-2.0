{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "test:unit": "cross-env NODE_ENV=test SKIP_DB_TESTS=1 jest --runInBand tests/unit",
    "test:integration": "cross-env NODE_ENV=test jest --runInBand tests/integration",
    "test:db": "cross-env NODE_ENV=test jest --runInBand tests/integration/db",
    "test:api": "cross-env NODE_ENV=test jest --runInBand tests/integration/api",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "test:db:commodities": "cross-env NODE_ENV=test jest --runInBand tests/integration/db/commodities.int.spec.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "pg": "^8.11.5",
    "supertest": "^6.3.4",
    "supabase": "^2.48.3"
  }
}
