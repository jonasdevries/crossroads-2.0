const cta = document.getElementById('cta');

if (cta) {
  // Provide an example interaction to kickstart the project structure.
  cta.addEventListener('click', () => {
    alert('Crossroads 2.O is ready for more features!');
  });
}
